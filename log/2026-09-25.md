# Day 023 log — 2026-09-25

Start timestamp (UTC epoch): 1790320351.

## Open

`git fetch origin` then compared local `claude/serene-fermi-87qq3u` to `origin/main`: identical
(both at `f4c89af`), working tree clean. `origin/claude/serene-fermi-87qq3u` does not exist yet (no
prior session on this designated branch) — nothing to reconcile. No open PR found via GitHub search
(`list_pull_requests` state=open returned empty), no stray unmerged branch. `STATE.md`'s `Day: 022`
/ `Last session: 2026-09-24, ended clean` matched `log/2026-09-24.md`.

The last commit on `main` (`f4c89af`, "Update INBOX.md") is a direct commit by Sam Den Haerynck
(`samdenhaerynck@hotmail.Com`), confirmed via `git show --stat`/author check — an owner change per
`OWNER.md`, needing no session PR/review. It added one new line to `INBOX.md`'s "New" section: a
QR-code phone-ordering idea for Belgian frituren/broodjeszaken.

Read `OWNER.md` in full, `RUNBOOK.md`, `INBOX.md`, `BACKLOG.md` (top-3 shortlist section), and
`DECISIONS.md`'s 2026-09-24 entry, per `STATE.md`'s "Read first" list.

## Work

`OWNER.md`'s Phase 0 sourcing rule: inbox ideas are screened in the next Phase 0 session, ahead of
anything else. Since this session is still Phase 0 (tournament round 1) and a new, unscreened inbox
line existed, this session's one unit of work was screening that idea — deferring the queued Deep
Check (`STATE.md`'s prior next action) to next session rather than combining both.

**Idea screened:** QR-code phone ordering for Belgian frituren/broodjeszaken (owner's own words:
scan a QR code, order and pay via phone, no kiosk/order screens, owner receives the order like an
online order, aimed at cutting high Belgian staffing costs). No `[must-try]`/`[i-know-users]` tag.

Quick web research (3 searches, 2 fetches, ~10 minutes, no subagent — appropriate for a single
idea rather than a batch of 8-12):

- **Hard disqualifiers:** none applies. Checked H3 specifically since QR-ordering-as-a-category is
  common — rejected, because H3 requires the *platform the target already runs on* to ship this
  free, and frituur owners have no common base platform; the free/cheap options found are
  third-party competing products, not a free tier of something they're already on. Checked H2 —
  the idea's "you already payed during the process" phrasing sounds custody-of-money-adjacent, but
  a v1 integrating as a client of an existing licensed PSP (Mollie/Payconiq/Stripe) rather than
  custodying funds directly is the standard SaaS pattern (same as any online-ordering product) and
  doesn't require the product itself to be licensed — a Build-phase implementation detail, not a
  hard disqualifier on the idea.
- **Demand (4/5):** horecawebservice.nl's own vendor comparison lists 20+ QR-ordering vendors in
  this category; Frituurwereld.nl and Get-Sides.nl cite concrete Belgian/Dutch staffing-cost-savings
  claims from QR ordering. Competitors count as demand evidence per the rubric.
- **WTP (4/5):** public pricing across competitors runs free/€1-day (Culio) through
  €9-€95/month (Menubly, OrderNow) up to ~€300/month for full platforms, some with an added 2-3%
  per-order fee.
- **Wedge (1/5):** **Frituurapp.be** already sells almost exactly this — QR phone ordering +
  payment + live order status — specifically to Belgian frituren/snackbars, plus three more
  Belgian-specific competitors (OrderBilly, Smartendr, Wazaltzijn) in the wider comparison. No gap
  (segment, price point, missing feature) was found that these don't already cover; recorded as "no
  evidence found" for a wedge, not invented.
- **Owner fit (1/5):** judgment call — the rubric's owner-fit dimension measures the owner's own
  reach/domain knowledge (5 = Optimizely/ADO/EU-consultancy daily work), not just whether the
  problem domain is Belgian. Nothing in `OWNER.md`'s owner-fit channels or the inbox line itself
  (untagged `[i-know-users]`) evidences a personal network into Belgian horeca/frituur ownership.
  Scored 1, not the 3 a surface reading of "Belgian SMB" might suggest.
- **Buildability (4/5):** a single-tenant demo (QR scan → menu → order → stubbed payment → owner
  order dashboard) fits comfortably in ~10 sessions.

**Total: 14/25.** No hard disqualifier, so per `OWNER.md` this graduates to `BACKLOG.md` rather
than being screened out — added as `frituur-qr-order`, ranked in the 14/25 tier, well below the
current 20/16/16 shortlist. Moved the inbox line from "New" to "Screened" in `INBOX.md` with a
one-line pointer to `BACKLOG.md`. Recorded the judgment call (owner-fit and wedge scoring
reasoning) in `DECISIONS.md`. `STATE.md`'s Deep Check next action carries over unchanged to day
024, since this idea didn't unseat any shortlisted candidate — flagged for the owner in case they
have context this search didn't surface (a specific wedge, or a personal connection into frituur
ownership that would change the owner-fit score).

## Close

This session's changes are docs/research only (`INBOX.md`, `BACKLOG.md`, `DECISIONS.md`,
`STATE.md`, this log) — no code, no `app/` changes. Per `OWNER.md`'s merge rules, docs-only changes
get one review round with up to 3 spot-checked citations rather than a full code review.

### Review — round 1

Ran an independent review via a fresh `general-purpose` subagent, given `OWNER.md`'s
"Self-correction limits" section verbatim plus the full diff, instructed to: read `OWNER.md` in
full, argue both directions of the verdict (could this idea's wedge/owner-fit be scored more
favorably, and separately, could it deserve an even lower score or a hard-disqualifier reframing),
spot-check up to 3 cited URLs by refetching them, and check for any weakening of `OWNER.md`'s
non-negotiables or Self-correction limits.

**Verdict: DO-NOT-MERGE** (two high-severity findings).

- **HIGH (fixed):** `BACKLOG.md`'s sourcing notes attributed a "€600/day" savings figure to
  Frituurwereld.nl and Get-Sides.nl. The reviewer refetched both pages directly and found no such
  figure on either — Frituurwereld.nl only supports the ~50% staffing-cost claim, and Get-Sides.nl's
  only monetary figure is "&gt;€1,000/month" (different number, different unit). This is an
  invented figure attributed to real sources, prohibited by `OWNER.md`'s evidence rules and the
  Self-correction-limits non-negotiable against inventing market/pricing data. **Fix:** removed the
  "€600/day" claim from `BACKLOG.md`, kept only the two claims each page actually supports, and
  added a one-line correction note in place rather than silently deleting it.
- **HIGH (fixed):** this log's Review section was committed with a placeholder
  (`[RESULT_PLACEHOLDER — replaced below before commit]`) instead of an actual verdict —
  `OWNER.md` requires the review verdict and findings recorded in the day log before merge. Fixed
  by replacing it with this round's real verdict and findings (this section).
- **MEDIUM (fixed):** `BACKLOG.md` mislabeled OrderNow's pricing as EUR ("Pro €44/mo and Ultra
  €95/mo") when the cited page (https://ordernowqr.com/pricing/) lists $44/mo and $95/mo (USD).
  Fixed to state USD explicitly.
- **MEDIUM (fixed):** the ranked table's inserted `frituur-qr-order` row grew the 14-point tier from
  3 to 4 members, but every rank label below it (13/13/13 → 12/12 → 11/11/11 → 10 → 9/9/9) was left
  unshifted, and the header still said "24 candidates" for what are now 25 rows. Fixed: header
  updated to "25 candidates," and standard competition ranking reapplied below the inserted row
  (14, 14, 14, 17, 17, 19, 19, 19, 22, 23, 23, 23) — no score or shortlist changes, purely numbering.
- **MEDIUM (not fixed, recorded as-is):** owner-fit scored 1/5 for `frituur-qr-order`, but
  `OWNER.md`'s rubric text literally reads "3 = adjacent (general .NET/TS dev tooling, **EU
  SMBs**)," and a Belgian frituur is an EU SMB by a plain reading — this session instead scored on
  the owner's *personal reach/network* into horeca specifically, a stricter, disclosed
  reinterpretation (see `DECISIONS.md` and `STATE.md`'s note to the owner asking them to correct it
  if wrong). The reviewer confirmed this is outcome-neutral: recomputing with owner-fit=3 gives
  16/25, which still loses the owner-fit tiebreak to `CMS12-UpgradeAssist`/`ADO-MultiOrg` (owner-fit
  5) and only ties the already-excluded `ci-migration-rollback-gate`. Left as a flagged judgment
  call rather than silently changed, per the reviewer's recommendation.
- **LOW (not fixed, recorded as-is):** the H3 check didn't test whether a Belgian mandatory
  cash-register (GKS) vendor already bundles free QR ordering on plans frituur owners already pay
  for (e.g. MplusKASSA, which appears in this session's own vendor list). Not required at
  Screen-level rigor for a non-shortlisted idea, and doesn't change today's outcome, but worth a
  note if this idea is ever revisited.
- **Checked and clean:** `STATE.md`'s day/date fields, `INBOX.md`'s move from "New" to "Screened,"
  score arithmetic (4+4+1+1+4=14), this session's git-state claims, diff scope (docs-only, no
  deploy/domain/account/credential activity), H2 reasoning (PSP-client integration, not fund
  custody), and the "only a hard disqualifier auto-kills" rule (wedge/owner-fit correctly lowered
  the rank rather than triggering a kill) — all confirmed sound, no changes needed.

Per `OWNER.md` merge rule 6, a high-severity finding requires a fresh re-review by a new subagent
before merging, after the fixes above.

### Review — round 2

Ran a second independent review via a fresh `general-purpose` subagent (not the round-1 reviewer),
given the same `OWNER.md` Self-correction-limits text, the updated diff, and round 1's findings
list, instructed to verify each round-1 fix directly (re-fetch the same URLs, re-check the table
arithmetic and numbering) rather than trust this log's account of them, and to flag anything round 1
missed.

**Verdict: [ROUND_2_PLACEHOLDER]**

## Summary 2026-09-25 (day 023, ~MIN min)
Idea: none | Phase: 0 Select (tournament round 1, step 2 Deep Check still next)
Did: Screened the owner's new INBOX.md idea (Belgian frituur QR phone ordering) ahead of the queued
Deep Check, per OWNER.md's inbox-priority rule.
Result: No hard disqualifier; scored 14/25 (wedge 1, owner fit 1 — Frituurapp.be and 20+ other QR-
ordering vendors already serve this space, no owner network into horeca). Added to BACKLOG.md as
`frituur-qr-order`, not shortlisted; day-022 shortlist and Deep Check next action unchanged.
Blocked: none.
Tomorrow: Deep Check the top-3 shortlist (BE-Peppol-Commerce, CMS12-UpgradeAssist, ADO-MultiOrg),
starting with BE-Peppol-Commerce's H2 question.
