# State
Day: 020
Idea: none
Phase: 0 Select
Slice: n/a
Next action: Day 021 must first replenish `BACKLOG.md` back up to at least 3 entries (it is at 2
after day 020's kill), then pick the new #1 and run Phase 1 Validate on it. This is a normal
Phase 0 session, not a sourcing-method-review session — the day-019 review's three-in-a-row
trigger has not fired again (day 020 is only the first kill since that review). When
replenishing, apply the two day-019 method updates: (1) do not budget real search time on forums
with a 2+-session blocked track record (Reddit generally; Mike Holt/ElectricianTalk/
ContractorTalk/JLC for construction/trades) — one quick attempt per session is fine, but expect
failure; (2) treat vendor/platform-owned surfaces (review sites, vendor feedback boards, Channel D
app-marketplace reviews, Freelancer.com) as the primary reachable channel, and expect demand
evidence found there to arrive already paired with an incumbent rather than as a clean open-field
signal — budget explicit separate time for the second-gap/sub-segment competitor check. Also apply
the lesson from today's kill: check whether a large, ubiquitous incumbent platform (not just named
competitors) already provides the exact deliverable natively, as a first-order check, before
scoring "reason to exist" — this is now the second idea (after the QBO one) where the native-
platform-feature check turned out to be decisive, so make it a standing first step for any idea
that plugs into a dominant platform (QBO, Shopify, Salesforce, etc.), not an afterthought.
Read first: `BACKLOG.md`'s 2026-09-22 entry and its two remaining candidates (#1 SOW generator,
#2 changelog generator), `killed/qbo-weekly-snapshot/REASON.md` for today's kill reasoning,
`RUNBOOK.md`'s "already-recurring free incumbent" and Channel D entries.
Notes for owner:
- Day 020 ran a normal Phase 1 Validate on `BACKLOG.md`'s #1 (QuickBooks weekly financial
  snapshot) and killed it: every task in the sourcing job posting turned out to be a free, native
  QuickBooks Online feature already, and the one residual gap already has a $9/mo third-party
  incumbent (Uncat). Resolved directly via `WebSearch`/`WebFetch` against Intuit's own product
  pages rather than a delegated subagent, since the flagged risk was narrow and primary-source-
  answerable. See `killed/qbo-weekly-snapshot/` for the full write-up.
- Did not re-verify the five disclosed non-competitors (Dext/Hubdoc, Chaser, Upflow, Zeni, Puzzle)
  `BACKLOG.md` also asked this session to check — the native-QBO-feature finding is independently
  sufficient to kill the idea regardless of their status, so re-confirming a conclusion that
  wouldn't change the verdict was judged not worth the session's time. Flagging this explicitly as
  a scope call this session made unilaterally, not something skipped silently.
- Consecutive kills is now 13 (0 shipped after 20 sessions) — carrying forward the same standing
  note prior sessions have recorded: every kill to date has died on concrete, verified evidence,
  not a process failure, but the owner may still want their own independent look at whether the
  Phase 1 bar itself is calibrated correctly, per the open question first raised day 017 and
  repeated in every session since (not something this session can decide unilaterally, per
  `OWNER.md`). This is only the first kill since the day-019 review, so the three-in-a-row trigger
  has not fired again yet.
- `BACKLOG.md` is now at 2 entries, below the routine's three-candidate floor; day 021's first
  task is to replenish before validating, per the recorded next action above.
- Per `OWNER.md`'s merge process: this session's work (a kill in `ideas/`→`killed/`,
  `BACKLOG.md`, `DECISIONS.md`, this file, and the day log — no app code) was reviewed by an
  independent subagent before merging; see today's log for the full verdict and findings.
Consecutive kills: 13
Last session: 2026-09-22, ended clean
