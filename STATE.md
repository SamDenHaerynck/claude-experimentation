# State
Day: 021
Idea: none
Phase: 0 Select
Slice: n/a
Next action: Day 022 must replenish `BACKLOG.md` back up to at least 3 entries (it is still at 2:
#1 SOW generator, #2 changelog generator), then pick the new #1 and run Phase 1 Validate on it —
same combined shape as today, but do not assume sourcing and Validate will collapse into one
session again; that only happened today because the decisive competitor evidence surfaced during
this session's own re-verification of the sourcing subagent's citations, not by design. Apply the
new `RUNBOOK.md` lesson from today: when independently re-`WebFetch`ing a cited source, explicitly
ask for every named product/competitor on the page, not just confirmation of the specific quote
the subagent flagged — a subagent can miss an adjacent competitor mention on the same page it
already cited. Also keep applying the day-019 method updates (avoid budgeting real time on
Reddit/trades forums; treat vendor/platform-owned surfaces — G2/Capterra reviews, vendor feedback
boards, app-marketplace reviews, Freelancer.com — as the primary reachable channel and expect
demand evidence to arrive paired with a competitor) and the day-020 lesson (check whether a
dominant platform the idea plugs into already provides the deliverable natively, as a first-order
step, before scoring "reason to exist").
Read first: `BACKLOG.md`'s 2026-09-23 entry and its two remaining candidates (#1 SOW generator, #2
changelog generator), `killed/cam-reconciliation-small-cre/REASON.md` for today's kill reasoning,
`RUNBOOK.md`'s newest entry (sourcing-subagent competitor miss) and the still-open "reason to
exist" auto-kill doctrine entry (do not resolve it unilaterally; flag under Notes for owner if it
becomes score-determinative).
Notes for owner:
- Day 021 sourced a new candidate (CAM reconciliation tool for small independent commercial
  property managers) via a delegated research subagent that checked ~17 verticals through general
  web search and found this the only one with solid evidence — worth noting that nearly every other
  vertical it checked turned out to already have 2+ named, currently-operating competitors within
  minutes of searching, the same colonization pattern behind most of this project's 14 kills.
- This session's own direct re-verification of the subagent's cited sources (standard practice
  since day 012/019) caught a competitor (PigJet) the subagent's own report had missed, despite it
  being named on the very forum page cited as evidence. Killed the idea the same session rather
  than deferring Validate to tomorrow, since the decisive evidence was already in hand — see
  `DECISIONS.md` for why this departs from the usual day-011-style "replenish now, validate later"
  split. Added a `RUNBOOK.md` entry so this failure mode (a subagent missing a same-page
  competitor) is checked for explicitly next time, not just for this one case.
- Consecutive kills is now 14 (0 shipped after 21 sessions). This is the second kill since the
  day-019 sourcing-method review — not yet a third-in-a-row, so the review trigger has not fired
  again — but the owner's own `OWNER.md` "Self-optimizing" instruction to review sourcing/validation
  after 3-in-a-row is worth flagging is getting close again, and the standing open question (first
  raised day 017, repeated every session since) of whether the Phase 1 bar itself is calibrated
  correctly remains unresolved and not something this session can decide unilaterally.
- `BACKLOG.md` is unchanged at 2 entries (below the routine's three-candidate floor) since today's
  sourced-and-killed candidate was never written into that file before being killed — day 022's
  first task is replenishment, per the recorded next action above.
- Per `OWNER.md`'s merge process: this session's work (a new sourcing+kill in `ideas/`→`killed/`,
  `BACKLOG.md`, `DECISIONS.md`, `RUNBOOK.md`, this file, and the day log — no app code) was reviewed
  by an independent subagent before merging; see today's log for the full verdict and findings.
Consecutive kills: 14
Last session: 2026-09-23, ended clean
